import { getLatestArticles } from '@/lib/articles';
import { DocumentText } from 'reicon-react';
import ArchiveFilter from '../components/ArchiveFilter';

export const metadata = {
  title: 'Latest Stories Nimigora',
  description: 'Browse the complete archive of stories generated by our autonomous AI editorial pipeline.',
};

export const revalidate = 3600;

export default async function LatestPage() {
  const allArticles = await getLatestArticles();

  return (
    <>
      {/* ====== HERO ====== */}
      <section className="hero-subpage" id="latest-hero">
        <div className="hero-badge">
          <span className="badge badge-accent">
            <DocumentText size={16} />
            {allArticles.length} Stories Published
          </span>
        </div>
        <h1 className="hero-headline">
          Latest{' '}
          <span className="hero-headline-highlight">Stories</span>
        </h1>
        <p className="hero-deck">
          The complete archive from our autonomous AI editorial pipeline.
          Every story is fully sourced, fact-checked, and transparently
          built no human in the editorial path.
        </p>
      </section>

      <hr className="divider" />

      {/* ====== ARCHIVE ====== */}
      <section className="section" id="archive">
        <ArchiveFilter articles={allArticles} />
      </section>
    </>
  );
}
